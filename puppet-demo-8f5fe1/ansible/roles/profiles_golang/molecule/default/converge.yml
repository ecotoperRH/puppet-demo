---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Include profiles_golang
      ansible.builtin.include_role: # noqa: L038 - role-under-test resolves at runtime; unresolved only when scanned in isolation
        name: profiles_golang
