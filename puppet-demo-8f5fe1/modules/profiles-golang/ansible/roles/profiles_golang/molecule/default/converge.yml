---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Placeholder — MoleculeAgent generates include_role converge
      ansible.builtin.debug:
        msg: "Role profiles_golang converge placeholder — will include_role: profiles_golang"
